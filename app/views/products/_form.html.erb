<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, "Nombre" %>
    <%= form.text_field :title, id: :product_title, :class =>'form-control' %>
  </div>

  <div class="field">
    <%= form.label :description, "Descripción" %>
    <%= form.text_area :description, id: :product_description, rows: 10, cols: 60, :class =>'form-control' %>
  </div>

  <div class="field">
    <%= form.label :image_url, "Imagen" %>
    <%= form.text_field :image_url, id: :product_image_url, :class =>'form-control' %>

  </div>

  <div class="field">
    <%= form.label :price, "Precio" %>
    <%= form.text_field :price, id: :product_price, :class =>'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
