<p id="notice"><%= notice %></p>

<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>
<h1 align="center">Ordenes</h1>


<div class="table-responsive">
  <table class="table">
    <thead>
    <tr>
      <th>Nombre</th>
      <th>Dirección</th>
      <th>Email</th>
      <th>Estado de Orden</th>
      <th>Tipo de pago</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.name %></td>
        <td><%= order.address %></td>
        <td><%= order.email %></td>
        <td><%= order.estado %></td>
        <td><%= order.pay_type %></td>
        <% if user_signed_in? %>
        <td><%= link_to 'Mostrar', order, :class=>'btn btn-default' %></td>
        <td><%= link_to 'Editar', edit_order_path(order), :class=>'btn btn-default' %></td>
        <td><%= link_to 'Borrar', order, method: :delete, data: { confirm: 'Estas seguro?' }, :class=>'btn btn-default' %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
  </table>
</div>
</div>

<br>
<% if user_signed_in? %>
    <%= link_to 'Productos', products_path, :class=>'btn btn-info btn-block' %>
    <%= link_to 'Nueva Orden', new_order_path, :class=>'btn btn-info btn-block' %>
<% end %>




</body>
</html>

